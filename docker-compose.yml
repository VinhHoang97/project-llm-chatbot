version: '3'
services:
  ollama-container:
    image: ollama/ollama:latest
    volumes:
      - ./data/ollama:/root/.ollama
    ports:
      - 11434:11434
    container_name: ollama

  streamlit-app:
    build: frontend/
    ports:
      - 8501:8501
    container_name: frontend